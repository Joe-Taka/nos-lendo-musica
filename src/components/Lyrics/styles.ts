import styled from 'styled-components';
import {Container} from '../styles';

export const LyricsContainer = styled(Container)`
  margin: 0 10vw;
  background-color: papayawhip;
  padding: 2rem;
  border-radius: 20px;
`;